spring.application.name=backend-servicex
server.port=8081
app.publicBaseUrl=https://heide-unideaed-michal.ngrok-free.dev
server.address=0.0.0.0

# Database Connection
spring.datasource.url=jdbc:postgresql://restaurant-admin-db.c3oec40wsyuk.eu-north-1.rds.amazonaws.com:5432/postgres
spring.datasource.username=postgres
spring.datasource.password=**Resto123
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate Settings
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Connection Pool Settings
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.connection-timeout=20000
spring.datasource.hikari.initialization-fail-timeout=1
spring.jpa.properties.hibernate.temp.use_jdbc_metadata_defaults=false

# Mail
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=flavorframe6@gmail.com
spring.mail.password=owmn doli asba clup
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Google OAuth2
spring.security.oauth2.client.registration.google.client-id=${GOOGLE_CLIENT_ID}
spring.security.oauth2.client.registration.google.client-secret=${GOOGLE_CLIENT_SECRET}
spring.security.oauth2.client.registration.google.scope=openid,profile,email
spring.security.oauth2.client.registration.google.redirect-uri=http://localhost:8081/login/oauth2/code/google

# Logging
logging.level.com.zaxxer.hikari=DEBUG
logging.level.org.springframework.jdbc.datasource=DEBUG
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.orm.jdbc.bind=TRACE

# AWS S3 Credentials
aws.access.key=${AWS_ACCESS_KEY}
aws.secret.key=${AWS_SECRET_KEY}

# Profile
spring.profiles.active=local

# =========================
# FILE UPLOAD CONFIG
# =========================
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=5MB
file.upload.max-photo-size-bytes=5242880
file.upload.logo-dir=uploads/logos
file.upload.photo-dir=uploads/photos

# =========================
# AI CONFIG — Groq (free, no credit card required)
# Sign up at https://console.groq.com and paste your key below.
# Key format: gsk_xxxxxxxxxxxxxxxxxxxx
# =========================
app.ai.apiKey=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
app.ai.endpoint=https://api.groq.com/openai/v1/chat/completions
app.ai.confidenceThreshold=${AI_CONFIDENCE_THRESHOLD:0.6}

# =========================
# AWS S3 CONFIG
# =========================
aws.s3.bucket-name=${AWS_S3_BUCKET_NAME:restautrant-photos-bucket}
aws.s3.region=${AWS_S3_REGION:eu-north-1}
aws.s3.photos-folder=photos/menu-items
aws.s3.logos-folder=photos/logos

# Migration
migration.enabled=false
migration.logos.enabled=false